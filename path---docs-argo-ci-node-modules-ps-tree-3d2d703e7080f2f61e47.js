webpackJsonp([0xd518926120af],{3058:function(e,s){e.exports={pathContext:{docHtml:'<h1>ps-tree</h1>\n<p><a href="https://travis-ci.org/indexzero/ps-tree"><img src="https://travis-ci.org/indexzero/ps-tree.svg" alt="Build Status"></a>\n<a href="https://codeclimate.com/github/indexzero/ps-tree"><img src="https://codeclimate.com/github/indexzero/ps-tree/badges/gpa.svg" alt="Code Climate"></a>\n<a href="https://codeclimate.com/github/indexzero/ps-tree"><img src="https://codeclimate.com/github/indexzero/ps-tree/badges/coverage.svg" alt="Test Coverage"></a>\n<a href="http://badge.fury.io/js/ps-tree"><img src="https://badge.fury.io/js/ps-tree.svg" alt="npm version"></a>\n<a href=""><img src="" alt="Node.js Version"></a>\n<a href="https://david-dm.org/indexzero/ps-tree"><img src="https://david-dm.org/indexzero/ps-tree.svg" alt="Dependency Status"></a>\n[node-version-image]: <a href="https://img.shields.io/node/v/listdirs.svg?style=flat">https://img.shields.io/node/v/listdirs.svg?style=flat</a>\n[node-version-url]: <a href="http://nodejs.org/download/">http://nodejs.org/download/</a></p>\n<p>Sometimes you cannot kill child processes like you would expect, this a feature of UNIX.</p>\n<blockquote>\n<p>in UNIX, a process may terminate by using the exit call, and it\'s parent process may wait for that event by using the wait system call. the wait system call returns the process identifier of a terminated child, so that the parent tell which of the possibly many children has terminated. If the parent terminates, however, all it\'s children have assigned as their new parent the init process. Thus, the children still have a parent to collect their status and execution statistics.\n(from "operating system concepts")</p>\n</blockquote>\n<p>Solution: use <code>ps-tree</code> to get all processes that a <code>child_process</code> may have started, so that they may all be terminated.</p>\n<pre><code class="language-js">var cp = require(\'child_process\'),\n    psTree = require(\'ps-tree\');\n\nvar child = cp.exec("node -e \'while (true);\'", function () {...});\n\n// This will not actually kill the child it will kill the `sh` process.\nchild.kill();\n</code></pre>\n<p>wtf? it\'s because exec actually works like this:</p>\n<pre><code class="language-js">function exec (cmd, cb) {\n  spawn(\'sh\', [\'-c\', cmd]);\n  ...\n}\n</code></pre>\n<p><code>sh</code> starts parses the command string and starts processes, and waits for them to terminate, but <code>exec</code> returns a process object with the pid of the <code>sh</code> process.\nHowever, since it is in <code>wait</code> mode killing it does not kill the children.</p>\n<p>Use <code>ps-tree</code> like this:</p>\n<pre><code class="language-js">var cp = require(\'child_process\'),\n    psTree = require(\'ps-tree\');\n\nvar child = cp.exec("node -e \'while (true);\'", function () { /*...*/ });\n\npsTree(child.pid, function (err, children) {\n  cp.spawn(\'kill\', [\'-9\'].concat(children.map(function (p) { return p.PID })));\n});\n</code></pre>\n<p>If you prefer to run <strong>psTree</strong> from the command line, use: <code>node ./bin/ps-tree.js</code></p>\n<h2>Cross Platform support</h2>\n<p>The <code>ps-tree</code> module behaves differently on *nix vs. Windows by spawning different programs and parsing their output. This is based on <code>process.platform</code> and not on checking to see if a <code>ps</code> compatible program exists on the system.</p>\n<h4>*nix</h4>\n<ol>\n<li>\n<p>" <defunct> " need to be striped</p>\n<pre><code class="language-bash">$ ps -A -o comm,ppid,pid,stat\nCOMMAND          PPID   PID STAT\nbbsd             2899 16958 Ss\nwatch &#x3C;defunct>  1914 16964 Z\nps              20688 16965 R+\n</code></pre>\n</li>\n</ol>\n<h3>Windows</h3>\n<ol>\n<li><code>wmic PROCESS WHERE ParentProcessId=4604 GET Name,ParentProcessId,ProcessId,Status)</code></li>\n<li>\n<p>The order of head columns is fixed</p>\n<pre><code class="language-shell">> wmic PROCESS GET Name,ProcessId,ParentProcessId,Status\nName                          ParentProcessId  ProcessId   Status\nSystem Idle Process           0                0\nSystem                        0                4\nsmss.exe                      4                228\n</code></pre>\n</li>\n</ol>\n<h3>LICENSE: MIT</h3>',docPath:"argo-ci/node_modules/ps-tree/readme",proj:"argo-ci"}}}});
//# sourceMappingURL=path---docs-argo-ci-node-modules-ps-tree-3d2d703e7080f2f61e47.js.map