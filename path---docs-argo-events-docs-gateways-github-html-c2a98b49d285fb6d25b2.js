webpackJsonp([55231542601876],{625:function(e,t){e.exports={pathContext:{docHtml:'<h1>Github</h1>\n<p>The gateway listens to events from GitHub. </p>\n<p>Refer <a href="https://developer.github.com/v3/activity/events/types/">here</a> for more information on type of events.</p>\n<h3>How to define an event source in confimap?</h3>\n<p>An entry in the gateway configmap corresponds to <a href="https://github.com/argoproj/argo-events/blob/30eaa296651e80b11ffef3b20464a08a2041eb09/gateways/community/github/config.go#L50-L73">this</a>.</p>\n<p>Refer <a href="https://developer.github.com/v3/repos/hooks/#get-single-hook">this</a> to understand the structure of webhook.</p>\n<p>The gateway spec defined in <code>examples</code> has a <code>serviceSpec</code>. This service is used to expose the gateway server and make it reachable from GitHub.</p>\n<p>The event payload dispatched from gateway contains the type of the event in the headers.</p>\n<p><strong>How to get the URL for the service?</strong></p>\n<p>Depending upon the Kubernetes provider, you can create the Ingress or Route. </p>\n<h2>Setup</h2>\n<p><strong>1. Install <a href="../../examples/gateways/github.yaml">Gateway</a></strong></p>\n<p>We are installing gateway before creating event sources. Because we need to have the gateway pod running and a service backed by the pod, so\nthat we can get the URL for the service. </p>\n<p>Make sure gateway pod and service is running</p>\n<p><strong>2. Install <a href="../../examples/event-sources/github.yaml">Event Source</a></strong></p>\n<p><strong>3. Install <a href="../../examples/sensors/github.yaml">Sensor</a></strong></p>\n<p>Make sure sensor pod is created.</p>\n<p><strong>4. Trigger Workflow</strong></p>\n<p>Depending upon the event you subscribe to, a workflow will be triggered.</p>',docPath:"argo-events/docs/gateways/github",proj:"argo-events"}}}});
//# sourceMappingURL=path---docs-argo-events-docs-gateways-github-html-c2a98b49d285fb6d25b2.js.map