webpackJsonp([0x726754cc3c02],{566:function(e,n){e.exports={pathContext:{docHtml:'<h2>Sensor Guide</h2>\n<p>Sensors define a set of dependencies (inputs) and actions (outputs). The sensor\'s actions will only be triggered after it\'s dependencies have been resolved.\n<a href="https://github.com/argoproj/argo-events/tree/eventing/examples/sensors">Sensor examples</a></p>\n<h3>Dependencies</h3>\n<p>Dependencies(also called signals) are defined as "gateway-name/specific-configuration"</p>\n<pre><code>signals:\n    - name: calendar-gateway/calendar.fooConfig\n    - name: webhook-gateway/webhook.barConfig\n</code></pre>\n<h3>Repeating the sensor</h3>\n<p>Sensor can be configured to rerun by setting repeat property to <code>true</code></p>\n<pre><code>spec:\n  repeat: true\n</code></pre>\n<h3>Triggers</h3>\n<p>Refer <a href="trigger-guide.html">Triggers</a> guide.</p>\n<h3>Event payload</h3>\n<p>Event payload of any signal can be passed to any trigger. To pass complete payload without applying any filter,\ndo not set <code>path</code>\ne.g.</p>\n<pre><code class="language-yaml">parameters:\n  - src:\n      signal: webhook-gateway/webhook.fooConfig\n      path:\n    dest: spec.templates.0.container.args.0\n</code></pre>\n<p>Complete example to pass payload from signal to trigger can be found <a href="https://github.com/argoproj/argo-events/blob/master/examples/sensors/webhook.yaml">here</a> </p>\n<p>To pass a particular field from payload to trigger, set <code>path</code>. e.g.</p>\n<pre><code class="language-yaml">parameters:\n  - src:\n      signal: webhook-gateway/webhook.fooConfig\n      path: name\n    dest: spec.templates.0.container.args.0\n</code></pre>\n<p>In above example, the object/value corresponding to key <code>name</code> will be passed to trigger.  </p>\n<h3>Filters</h3>\n<p>Additionally, you can apply filters on the payload.</p>\n<p>There are 3 types of filters:</p>\n<table>\n<thead>\n<tr>\n<th>Type</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Time</td>\n<td>Filters the signal based on time constraints</td>\n</tr>\n<tr>\n<td>EventContext</td>\n<td>Filters metadata that provides circumstantial information about the signal.</td>\n</tr>\n<tr>\n<td>Data</td>\n<td>Describes constraints and filters for payload</td>\n</tr>\n</tbody>\n</table>\n<h4>Time Filter</h4>\n<pre><code>filters:\n        time:\n          start: "2016-05-10T15:04:05Z07:00"\n          stop: "2020-01-02T15:04:05Z07:00"\n</code></pre>\n<h4>EventContext Filter</h4>\n<pre><code>filters:\n        context:\n            source:\n                host: amazon.com\n            contentType: application/json\n</code></pre>\n<h4>Data filter</h4>\n<pre><code>filters:\n        data:\n            - path: bucket\n              type: string\n              value: argo-workflow-input\n</code></pre>',docPath:"argo-events/docs/sensor-guide",proj:"argo-events"}}}});
//# sourceMappingURL=path---docs-argo-events-docs-sensor-guide-html-369923717aade50623fd.js.map