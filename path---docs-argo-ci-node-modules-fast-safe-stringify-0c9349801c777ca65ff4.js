webpackJsonp([77465016736534],{2326:function(e,n){e.exports={pathContext:{docHtml:'<h1>fast-safe-stringify</h1>\n<p>Safe and fast serialization alternative to <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify">JSON.stringify</a>.</p>\n<p>Gracefully handles circular structures instead of throwing.</p>\n<p>Provides a deterministic ("stable") version as well that will also gracefully\nhandle circular structures. See the example below for further information.</p>\n<h2>Usage</h2>\n<p>The same as <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify">JSON.stringify</a>.</p>\n<p><code>stringify(value[, replacer[, space]])</code></p>\n<pre><code class="language-js">const safeStringify = require(\'fast-safe-stringify\')\nconst o = { a: 1 }\no.o = o\n\nconsole.log(safeStringify(o))\n// \'{"a":1,"o":"[Circular]"}\'\nconsole.log(JSON.stringify(o))\n// TypeError: Converting circular structure to JSON\n\nfunction replacer(key, value) {\n  console.log(\'Key:\', JSON.stringify(key), \'Value:\', JSON.stringify(value))\n  // Remove the circular structure\n  if (value === \'[Circular]\') {\n    return\n  }\n  return value\n}\nconst serialized = safeStringify(o, replacer, 2)\n// Key: "" Value: {"a":1,"o":"[Circular]"}\n// Key: "a" Value: 1\n// Key: "o" Value: "[Circular]"\nconsole.log(serialized)\n// {\n//  "a": 1\n// }\n</code></pre>\n<p>Using the deterministic version also works the same:</p>\n<pre><code class="language-js">const safeStringify = require(\'fast-safe-stringify\')\nconst o = { b: 1, a: 0 }\no.o = o\n\nconsole.log(safeStringify(o))\n// \'{"b":1,"a":0,"o":"[Circular]"}\'\nconsole.log(safeStringify.stableStringify(o))\n// \'{"a":0,"b":1,"o":"[Circular]"}\'\nconsole.log(JSON.stringify(o))\n// TypeError: Converting circular structure to JSON\n</code></pre>\n<p>A faster and side-effect free implementation is available in the\n<a href="">safe-stable-stringify</a> module. However it is still considered experimental\ndue to a new and more complex implementation.</p>\n<h2>Differences to JSON.stringify</h2>\n<p>In general the behavior is identical to <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify">JSON.stringify</a>. The <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify#The%20replacer%20parameter"><code>replacer</code></a>\nand <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify#The%20space%20argument"><code>space</code></a> options are also available.</p>\n<p>A few exceptions exist to <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify">JSON.stringify</a> while using <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify#toJSON()_behavior"><code>toJSON</code></a> or\n<a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify#The%20replacer%20parameter"><code>replacer</code></a>:</p>\n<h3>Regular safe stringify</h3>\n<ul>\n<li>\n<p>Manipulating a circular structure of the passed in value in a <code>toJSON</code> or the\n<code>replacer</code> is not possible! It is possible for any other value and property.</p>\n</li>\n<li>\n<p>In case a circular structure is detected and the <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify#The%20replacer%20parameter"><code>replacer</code></a> is used it\nwill receive the string <code>[Circular]</code> as the argument instead of the circular\nobject itself.</p>\n</li>\n</ul>\n<h3>Deterministic ("stable") safe stringify</h3>\n<ul>\n<li>\n<p>Manipulating the input object either in a <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify#toJSON()_behavior"><code>toJSON</code></a> or the <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify#The%20replacer%20parameter"><code>replacer</code></a>\nfunction will not have any effect on the output. The output entirely relies on\nthe shape the input value had at the point passed to the stringify function!</p>\n</li>\n<li>\n<p>In case a circular structure is detected and the <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON/stringify#The%20replacer%20parameter"><code>replacer</code></a> is used it\nwill receive the string <code>[Circular]</code> as the argument instead of the circular\nobject itself.</p>\n</li>\n</ul>\n<p>A side effect free variation without these limitations can be found as well\n(<a href="https://github.com/BridgeAR/safe-stable-stringify"><code>safe-stable-stringify</code></a>). It is also faster than the current\nimplementation. It is still considered experimental due to a new and more\ncomplex implementation.</p>\n<h2>Benchmarks</h2>\n<p>Although not JSON, the Node.js <code>util.inspect</code> method can be used for similar\npurposes (e.g. logging) and also handles circular references.</p>\n<p>Here we compare <code>fast-safe-stringify</code> with some alternatives:\n(Lenovo T450s with a i7-5600U CPU using Node.js 8.9.4)</p>\n<pre><code class="language-md">fast-safe-stringify:   simple object x 1,121,497 ops/sec ±0.75% (97 runs sampled)\nfast-safe-stringify:   circular      x 560,126 ops/sec ±0.64% (96 runs sampled)\nfast-safe-stringify:   deep          x 32,472 ops/sec ±0.57% (95 runs sampled)\nfast-safe-stringify:   deep circular x 32,513 ops/sec ±0.80% (92 runs sampled)\n\nutil.inspect:          simple object x 272,837 ops/sec ±1.48% (90 runs sampled)\nutil.inspect:          circular      x 116,896 ops/sec ±1.19% (95 runs sampled)\nutil.inspect:          deep          x 19,382 ops/sec ±0.66% (92 runs sampled)\nutil.inspect:          deep circular x 18,717 ops/sec ±0.63% (96 runs sampled)\n\njson-stringify-safe:   simple object x 233,621 ops/sec ±0.97% (94 runs sampled)\njson-stringify-safe:   circular      x 110,409 ops/sec ±1.85% (95 runs sampled)\njson-stringify-safe:   deep          x 8,705 ops/sec ±0.87% (96 runs sampled)\njson-stringify-safe:   deep circular x 8,336 ops/sec ±2.20% (93 runs sampled)\n</code></pre>\n<p>For stable stringify comparisons, see the performance benchmarks in the\n<a href="https://github.com/BridgeAR/safe-stable-stringify"><code>safe-stable-stringify</code></a> readme.</p>\n<h2>Protip</h2>\n<p>Whether <code>fast-safe-stringify</code> or alternatives are used: if the use case\nconsists of deeply nested objects without circular references the following\npattern will give best results.\nShallow or one level nested objects on the other hand will slow down with it.\nIt is entirely dependant on the use case.</p>\n<pre><code class="language-js">const stringify = require(\'fast-safe-stringify\')\n\nfunction tryJSONStringify (obj) {\n  try { return JSON.stringify(obj) } catch (_) {}\n}\n\nconst serializedString = tryJSONStringify(deep) || stringify(deep)\n</code></pre>\n<h2>Acknowledgements</h2>\n<p>Sponsored by <a href="http://nearform.com">nearForm</a></p>\n<h2>License</h2>\n<p>MIT</p>',docPath:"argo-ci/node_modules/fast-safe-stringify/readme",proj:"argo-ci"}}}});
//# sourceMappingURL=path---docs-argo-ci-node-modules-fast-safe-stringify-0c9349801c777ca65ff4.js.map